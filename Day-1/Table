#include<stdio.h>
int main()
{
    int num, ran,pro=1;
    printf("Enter the number: ");
    scanf("%d",&num);

    printf("Enter the range: ");
    scanf("%d",&ran);

    for(int i=1; i<=ran; i++){
        printf("%d X %d = %d\n",num,i,num*i);
    }

    return 0;
}
