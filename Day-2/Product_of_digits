#include<stdio.h>
int main()
{
    int num, pro=1, rem;
    printf("Enter the number: ");
    scanf("%d",&num);

    while(num!=0){
        rem=num%10;
        pro *= rem;
        num=num/10;
    }

    printf("Product of digits of number: %d",pro);

    return 0;
}
